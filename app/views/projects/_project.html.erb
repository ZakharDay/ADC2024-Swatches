<div id="<%= dom_id project %>">
  <p>
    <strong>Name:</strong>
    <%= project.name %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= project.description %>
  </p>

  <div class="fillsTeaser">
    <% project.swatches.each do |swatch| %>
      <% swatch.fills.each do |fill| %>
        <%= render "projects/fill", fill: fill %>
      <% end %>
    <% end %>
  </div>
</div>
